using System;
using StudentProgram;

class StudentArray{
    /*public static void Main(string[] args)
    {
        int studentCount = 3;
        Student[] students = new Student[studentCount];// created the array of student class
        for (int i = 0; i < studentCount; i++)
        {
            students[i] = new Student();// withthe new keyword object willl be created
            Console.WriteLine("Enter the Name of Student");
            students[i].Name = Console.ReadLine();


            Console.WriteLine("Enter th  Age Of Student");
            students[i].Age = Convert.ToInt32(Console.ReadLine());

            Console.WriteLine("Enter the No of Subject Marks you want to save");
            int subjectCount = Convert.ToInt32(Console.ReadLine());
            students[i].SubjectMarks = new int[subjectCount];

            for (int j = 0; j < subjectCount; j++)
            {
                Console.WriteLine("Enter the Marks for subjects");
                students[i].SubjectMarks[j] = Convert.ToInt32(Console.ReadLine());
            }


        }
        Console.WriteLine("Students obj details are given below");
        foreach (Student s in students)
        {
            Console.WriteLine(s.Name);
            Console.WriteLine(s.Age);

            foreach (int marks in s.SubjectMarks)
            {
                Console.WriteLine("Marks of Students are:" + marks);

            }
        }


    }*/
}